# Daily Development Log

**Project:** DSLibrary  
**Date:** 09 July 2026

---

## Objective

The objective for today's session was to continue building the DSLibrary project by implementing a generic Linked List using C++ templates, organizing the project into a modular structure, configuring the build system with CMake, and integrating the project with GitHub.

---

## Work Completed

### Linked List Implementation

Successfully implemented a generic singly linked list using C++ templates.

Implemented the following features:

- Templated `Node` class
- Templated `LinkedList` class
- Dynamic memory allocation
- `push()` operation
- `pop()` operation
- `operator[]` for element access
- Destructor for memory cleanup
- Exception handling for invalid operations

---

### Project Refactoring

Organized the project into separate directories for better maintainability.

```
DSLibrary/
├── build/
├── docs/
├── include/
│   ├── List.h
│   ├── List.tpp
│   ├── Vector.h
│   └── Vector.tpp
├── objs/
├── src/
│   └── main.cpp
└── CMakeLists.txt
```

Separated declarations and implementations:

- `List.h`
- `List.tpp`
- `Vector.h`
- `Vector.tpp`

This makes the project modular and easier to extend.

---

## CMake Configuration

Configured the project to build using CMake.

Commands used:

```bash
cmake -S . -B build
cmake --build build
```

Successfully generated the executable and verified the build process.

---

## Testing

Created a custom `Student` class to test template functionality.

Test cases performed:

- Insert multiple objects
- Access objects using `operator[]`
- Verify dynamic memory allocation
- Test destructor execution
- Validate exception handling

All tests completed successfully.

---

## Git and GitHub

Integrated the project with GitHub.

### Issue Encountered

GitHub rejected the push because the repository history contained a `.dmg` file larger than the 100 MB file size limit.

### Resolution

- Located the large file in Git history.
- Removed it using `git filter-repo`.
- Re-added the Git remote after history rewrite.
- Successfully restored the repository for future pushes.

---

## Challenges Faced

- GitHub push rejected due to large file.
- `git filter-repo` removed the `origin` remote.
- Initially attempted to run the executable using the wrong target name generated by CMake.

---

## Skills Improved

- C++ Templates
- Dynamic Memory Management
- Placement `new`
- Manual Object Lifetime Management
- Exception Handling
- Project Organization
- CMake Build System
- Git Version Control
- GitHub Repository Management

---

## Current Project Status

- ✅ Linked List implementation completed.
- ✅ Modular project structure created.
- ✅ CMake build system configured.
- ✅ Project successfully builds and runs.
- ✅ Repository synchronized with GitHub.

---

## Next Day's Plan

- Implement the `Vector` container.
- Add iterator support.
- Implement `Stack` using `Vector`.
- Implement `Queue` and `Deque`.
- Write unit tests.
- Improve project documentation.

---

**Session Summary**

Today's development focused on building the foundational structure of the DSLibrary project. A generic Linked List was implemented using C++ templates, the project was reorganized into a scalable directory structure, CMake was configured for cross-platform builds, and GitHub integration issues caused by large files were resolved. The project is now in a stable state and ready for implementing additional data structures.